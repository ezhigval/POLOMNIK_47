/** Desktop chrome (header nav, hide burger). Keep in sync with Tailwind `lg`. */
export const DESKTOP_MIN_WIDTH_PX = 1024;

export const desktopMediaQuery = `(min-width: ${DESKTOP_MIN_WIDTH_PX}px)`;
