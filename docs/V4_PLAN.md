# План линейки v4

Канон: [ROADMAP.md](ROADMAP.md) · [STATUS.md](STATUS.md) · [AGENTS.md](../AGENTS.md).  
База прода: тег **`v3.0.0`** (2026-08-26, v3 code-complete + контент).  
Чеклист владельца: [V4_OWNER_SETUP.md](V4_OWNER_SETUP.md). Исторический план v3 (закрыт): [V3_PLAN.md](V3_PLAN.md).

v3 заморожен. Новые фичи и долг — сюда. Сверка **2026-08-27**: этапы **1, 9–15 и 16–19** в `main` и на проде. Этапы **2–8** (долг, кабинеты владельца, live-адаптеры, оплата, юридичка, ИИ) **не** закрыты. Актуальная таблица «что упустили»: [STATUS.md](STATUS.md).

Правила те же, что в v3:

- Не выдумывать цены, юридичку, тексты туров, статусы заявок.
- Без ключа адаптер `Configured()==false`, сайт жив.
- Live Bitrix24 / 1С и живой эквайринг — только по явной просьбе владельца.
- Telegram-уведомления на проде не выключать.
- Секреты только в gitignored `.env.production`. Несекретные поля сайта — админка «Настройки».
- Не делать линкфармы, фейковые отзывы, покупные биржи ссылок.

```text
0 freeze v3 → 1 зачистка → 9…11 продукт → 12 автодеплой (workflow есть, SSH-ключ не совпадает)
                         → 13 флаги → 14 горящий/скрытие → 15 навигация
                         → 16 цена и payment_status → 17 Тихвинский путь
                         → 18 места регулярного тура → 19 мобильный UX
                         → 2–8 ждут владельца (не вычёркивать)
```

Продукт **9–19** на проде. **Этап 12:** workflow после зелёного CI на `main`; пока секрет SSH неверный — `make deploy`. Этапы 2–8 — инвентарь недоделок; не вычёркивать.

| Этап | Зачем сейчас | Ключи / действия владельца | Статус |
|------|----------------|----------------------------|--------|
| 0 База freeze | Не смешать WIP с закрытым v3 | нет | **готово** (`v3.0.0`) |
| 1 Зачистка | Баги, следы ИИ, безопасная оптимизация | нет | **код этапа 1** (cleanup, в `main`) |
| 9 Сессия и cookies | Сохранить вход и просмотренные туры | нет (существующий кабинет) | **готово** |
| 10 Главная: компактные новости + SEO | Та же плотность, что блок туров; мета; Wordstat → запросы на живые страницы; **компакт hero** (без CTA-кнопок и stat-плашек в шапке) | Вебмастер / Wordstat / Search Console — кабинет владельца | **готово** (компакт список, SEO pass, `seo-wordstat.ts`) |
| 11 Лента `/news` ≈ VK | Единая прокрутка; лайки и комментарии; URL кликабельны; фото на весь экран | нет (cookie `palomnik_visitor`, JWT для комментариев) | **готово** (goose 00028) |
| 12 Автодеплой + миграции | Push/мерж в `main` → один деплой; goose только `up` | **приватный** SSH в `DEPLOY_SSH_KEY`; сейчас Actions падает `Permission denied` | workflow **готово**; live-деплой из GHA **нет** |
| 13 Флаги туров | Админка: активный, регулярный, популярный, овербукинг | не назначать цену регулярному | **готово** |
| 14 Горящий тур и скрытие прошедших | Окно 7 дней до `date_start`; скидка `hot_discount_percent` в карточке тура | не путать с «Популярный» | **готово** (goose 00030) |
| 15 Навигация UX | «Назад», крошки, активное меню; форма заявки на карточке тура сверху | нет | **готово** |
| 16 Цена и статус оплаты | Цена на витрине если > 0; `payment_status` заявки | live-эквайринг не включать | **готово** (goose 00029) |
| 17 Тихвинский путь | Карточка тура + CTA главной → `/search?route=tikhvin-path` | не выдумывать цену | **готово** (goose 00031, #45) |
| 18 Места регулярного тура | `slots_left=0` + заполненное «всего» ≠ sold-out | закрыть набор = снять «Активный» | **готово** (goose 00032) |
| 19 Мобильный UX | Поля дат в карточке поиска; бургер без «Чат поддержки»; превью новости = первое фото | нет | **готово** (#46) |
| 2 Долг v2/v3 | То, что осталось без новых правил | внутренняя цена регулярного; SMTP/OAuth; капча | частично (env, места, витрина); остальное — STATUS |
| 3 Кабинеты владельца | То, что зависит только от Валентина | Вебмастер, Search Console, OAuth, SMTP, sms.ru, MX/SPF/DKIM, карта, ссылка с епархии | не код |
| 4 Live-адаптеры | Когда ключ вписан | Messenger / Publisher / AI / Captcha / S3 | ждать просьбы; на проде `noop` |
| 5 Оплата | Когда будут статусы и договор | `AWAITING_PAYMENT` / `PAID` — решение владельца; ЮKassa/Сбер | не начинать без статусов |
| 6 Юридичка | Тексты и реквизиты | сертификация юристом; `OPERATOR_*` значения (не выдумывать ИНН/ОГРН) | placeholders |
| 7 ИИ-звонки | Бывший «v4» из V3_PLAN | телефония + промпты владельца | **не этап 1** |
| 8 ИИ-продавец | Бывший «v4» из V3_PLAN | YandexGPT live + правила продаж | **не этап 1** |

---

## Этап 0 — база freeze

Тег `v3.0.0` на `main`. Goose **25**. На проде: этапы v3 0–10, новости (святыни, икона в Москву), закрепление 1+2, `is_regular`, Метрика **111985266**, Telegram live.

Не переоткрывать закрытые v3-PR. **Не мержить PR #27** (устаревшее закрепление; живое — #31). Черновик #25 (пересылка почты) — поле уже в «Настройках» на `main`, дубль не нужен.

---

## Этап 1 — баги, следы ИИ, оптимизация

Первый обязательный шаг v4. Без новых продуктовых фич и без выдуманных бизнес-правил.

Сделано в коде этапа 1:

- Удалена мёртвая форма `telegram-settings-form` (админка давно на канале+адресе).
- `.env.example` / `.env.production.example` без дублей OAuth и без «Stage E»; пустые плейсхолдеры и ссылки на доки.
- Compose прокидывает `OPERATOR_*` и `NEXT_PUBLIC_OPERATOR_*` (пустые = плейсхолдеры в коде).
- Worker получает `PUBLISHER_*` (due SMM) и `BACKUP_LAST_PATH` (watchdog).
- Из compose убран неиспользуемый `GOOGLE_CLIENT_*` (кнопка Google скрыта).
- В «Настройках» явная памятка: секреты и Метрика только env, не вторая правда в БД.

Дальше по этапу 1 — только доказуемые баги (тесты, битые ссылки, leftover goose). Не «чинить» цену регулярного тура и не вводить `AWAITING_PAYMENT`.

Проверки: `cd backend && go test ./... && go vet ./...` · `cd frontend && npm run lint && npm run build`.

---

## Этап 9 — сессия и cookies (после этапа 1)

Сохранить авторизацию и список **просмотренных туров**. Второй логин не заводить.

- Вход: существующий кабинет (`/account`, JWT-cookie `palomnik_token`, OAuth/пароль/callcheck как сейчас). Не параллельная учётка и не отдельный «гость-аккаунт» ради ленты.
- Cookie сессии: httpOnly, SameSite, Secure на проде — как у текущего `palomnik_token`. Срок не выдумывать заново, если текущие 7 дней ок; менять только по просьбе владельца.
- Просмотренные туры: cookie (и при наличии сессии — тот же кабинет, без второй базы «гостей»). Список для UX («вы смотрели»), не аналитика вместо Метрики.
- Согласия cookie-баннера (#22) не подменять: аналитика по-прежнему после «Принять все». Существенные cookie сессии/просмотров — в рамках уже принятой cookie policy, тексты юридички не переписывать.

Код: не дублировать auth в Fiber-handler и не хранить JWT в `localStorage`, если уже есть httpOnly cookie.

---

## Этап 10 — компактные новости на главной + SEO pass

Два независимых куска одного этапа; оба после cookies, до ленты `/news`.

### Главная: плотность как у туров

Сейчас блок новостей на главной — тот же magazine **1+2**, что и `/news` (`NewsFeed` / `splitNewsLayout`, крупные карточки). Блок туров — компактное расписание (`TourSchedule`, мало вертикали).

Нужно: компактный список новостей на главной, мало высоты, не 1+2. Layout 1+2 **на главной** не использовать. Закрепление звёздочкой в админке (#31) не отменять: порядок/выбор можно оставить, визуал — плотный список. Тексты заголовков и лидов не выдумывать.

Полная лента — этап 11, не этот блок.

### Hero: компактная шапка (сделано в коде)

- Убрать из hero CTA-кнопки («Выбрать тур», «Как записаться» и любые `primaryCta` / `secondaryCta` из CMS).
- Оставить: eyebrow, заголовок, подзаголовок, виджет поиска/заявки (`TripSearchConstructor`).
- Убрать stat-плашки из hero (12+ лет, 3000+ паломников и т.д.).
- Те же цифры и подписи — в блок «О службе» (`/#about`), без новых чисел; CMS seed и fallback `trustStats` перенесены.

### SEO на всех публичных страницах

Проход по публичным URL: `title`, `description`, canonical, Open Graph, JSON-LD, sitemap, человекочитаемые slug.

База уже есть (#30): slug туров, sitemap, robots, каноникал, OG, JSON-LD на части страниц. Этап — **закрыть дыры** (страницы без title/description/canonical/OG/JSON-LD, sitemap без живых slug). Кабинеты `/account`, `/management`, чат — по-прежнему `noindex`.

**Не выдумывать копирайт.** Title/description — из CMS, карточки тура/новости, `siteConfig` (имя и слоган владельца). Пустые мета не заполнять «нейросетевым» текстом.

### Wordstat и семантика (тот же этап 10)

Раскрутка выдачи — не ещё один мета-тег, а **запросы людей → живая страница**.

- Снять частоту в [Яндекс.Wordstat](https://wordstat.yandex.ru/) (регион СПб и ЛО): паломничество, паломнические туры / поездки, святыни, Тихвин / икона / епархия, «из Санкт-Петербурга». Лист и посадка на URL: **[SEO_WORDSTAT.md](SEO_WORDSTAT.md)**. Цифры — только из кабинета, не с потолка.
- Внедрить рабочие фразы в title, description, H1 и JSON-LD **существующих** `/`, `/search`, `/tours/{slug}`, `/news` и статей. Нет тура в Оптину — нет посадочной «под Оптину».
- Фильтры `/search?q=` по-прежнему не в индексе. Линкфармы и переспам — нет.
- Директ может брать те же проверенные запросы; цена в объявлении только с сайта.

Клики «Проверить» в Яндекс.Вебмастере и Google Search Console, отправка sitemap — **только владелец** ([V4_OWNER_SETUP.md](V4_OWNER_SETUP.md), [SEO_ADS.md](SEO_ADS.md)). Не делать админ-экран «проверить Вебмастер». Линкфармы и биржи ссылок — нет.

---

## Этап 11 — лента `/news` ≈ VK (лайки, комментарии, ссылки, фото)

**Готово в коде** (goose **00028**, после этапов 9–10).

Страница `/news` — единая непрерывная прокрутка (`NewsInfiniteFeed`), не magazine 1+2. Карточка `/news/[slug]` остаётся для шаринга и SEO.

Сделано:

- Лайки и комментарии. **Репост в этом срезе нет.**
- Комментарии: только с регистрацией (тот же кабинет этапа 9). Гость видит ленту и ссылку «Войдите, чтобы комментировать».
- Лайки: регистрация не нужна. Cookie `palomnik_visitor` (httpOnly, 1 год), API `/api/v1/news/:slug/likes`.
- Счётчики и toggle лайка — `NewsEngagementService` (domain/application); Postgres `news_likes`, `news_comments`.
- **Ссылки в тексте**: `linkifyText` — `http://` / `https://` → кликабельные ссылки (`rel="noopener noreferrer"`).
- **Фото**: в ленте — первое; на `/news/[slug]` — все (lightbox). Превью не дублирует галерею.

Гексагон: `adapters → application → domain`. Хендлер разбирает HTTP и отдаёт DTO; фронт — UX, не машина лайков.

---

## Этап 12 — автодеплой с `main` и аккуратные миграции

После этапа 1, **параллельно** с продуктом 9–11 (не блокирует ленту). Сейчас деплой вручную: `make deploy` с машины агента/владельца. Цель: **мерж в `main` после зелёного CI = выкладка на https://tikhvin-palomnik.ru**, без второго ручного шага.

### Автодеплой

- Триггер: успешный CI на `main` (не на feature-ветках, не на PR).
- Один деплой за раз: `concurrency` в GitHub Actions, очередь, не два `docker compose build` на ВМ одновременно.
- Тот же путь, что сейчас: `deploy/yandex/deploy.sh` / `make deploy`. Каталог `/opt/polomnik`. **Не** `compose down -v`.
- **Не** rsync `.env.production` с раннера на ВМ (секреты живут только на сервере). Уже в `deploy.sh`.
- После `up`: `https://api.tikhvin-palomnik.ru/health/ready` = 200. При падении health — не считать релиз успешным.
- Откат: checkout известного тега → тот же пайплайн. Миграции только аддитивные, поэтому «назад» по данным не через `goose down` на проде.
- Секреты раннера — GitHub Actions Secrets (имена в [V4_OWNER_SETUP.md](V4_OWNER_SETUP.md)), не в репозитории. SSH-ключ деплоя ≠ токен Cloudflare и ≠ `ADMIN_TOKEN`.

### Миграции (goose) без потери данных

- На проде только **`goose up`**. Не `down`, не `reset`, не ручной `DROP`.
- Новый файл = следующий свободный номер. **Не править уже применённые** `000NN_*.sql` на проде.
- Данные с `;` внутри строк (новости, HTML) — только в `-- +goose StatementBegin` / `StatementEnd` (урок 00025).
- `UPDATE` существующих рядов — отдельная новая миграция (как 00026 «духовник»→«священник»), не переписывание старого INSERT.
- Postgres named volume не трогать. Сид на проде не затирает живые туры/новости/заявки (compose seed идемпотентный / no-op на заполненной БД — не включать destructive seed).
- После миграций, которые меняют публичные туры/новости: сбросить Redis `tours:namespace` / `news:namespace`, иначе главная до 5 минут отдаёт старое.

**В коде (этап 12):** `.github/workflows/deploy.yml` — после зелёного CI на push в `main` (и `workflow_dispatch`), `concurrency: production-deploy`, `DEPLOY_CI=1`, health `https://api.tikhvin-palomnik.ru/health/ready`. Секрет: **`DEPLOY_SSH_KEY`** (приватный ключ). Опционально **`DEPLOY_SSH_HOST`**. На 2026-08-27 workflow **падает** `Permission denied (publickey)` — ключ в Secrets не тот, что в `authorized_keys` на ВМ. Пока так: **`make deploy`**, не два параллельно.

---

## Этап 13 — флаги тура в админке (корректировка)

После этапа 1, **параллельно** с 9–12. Поля уже есть (`is_active`, `is_regular`, `is_hot`, `overbooking_enabled`). Владелец зафиксировал смысл. Не выдумывать цены.

Тур **регулярный**, если он существует как факт предложения, но **конкретный выезд не запланирован**. Тогда на витрине и в карточке **нет цены и нет дат** (поля в админке при галочке скрывать, на сайте не показывать «0 ₽» и пустой календарь). Заявку принимать тем же потоком; сумму не назначать, пока владелец не скажет иначе (сейчас `total_price=0`).

Четыре независимые галочки (можно сочетать, кроме скрытого неактивного):

| Админка | Поле | На сайте |
|---------|------|----------|
| **Активный** | `is_active` | Виден в каталоге и по прямой ссылке. Снят — скрыт с витрины. |
| **Регулярный** | `is_regular` | Без даты и без цены. Не путать с «скрыт»: регулярный + активный = виден. |
| **Популярный** | `is_hot` | Подсветка карточки и **тег «Популярный»** (не «Hot» / не обязательно «Хит сезона»). |
| **Овербукинг** | `overbooking_enabled` | Когда места кончились (`slots_left=0`), заявку **всё равно** можно оставить; бронь помечается `overbooked`. Не уводить карточку в «нельзя записаться», если галочка включена. |

Не смешивать с закреплением новостей. Не автопомечать Успение и листовки регулярными. Листовки без цен по-прежнему `is_active=false`, пока владелец не даст дату/цену **или** явно не включит «Регулярный» + «Активный».

Код этапа: подписи в админке на русском; тег популярности единообразно «Популярный»; регулярный — не рендерить блоки цены/дат на главной, `/search`, карточке, JSON-LD Offer/event с фиктивной датой.

---

## Этап 14 — горящий тур и скрытие прошедших

После этапа 13. Не путать с `is_hot` («Популярный»).

| Условие | Поведение |
|---------|-----------|
| **Окно горящего тура** (сегодня ∈ [`date_start` − 7 дней; `date_start`], UTC, включительно) | На витрине `is_burning=true`, тег **«Горящий тур»**, скидка от базовой цены если в настройках тура задан `hot_discount_percent` > 0. После `date_start` бейдж снимается |
| **Дата окончания прошла** (`date_end` < сегодня) | Worker раз в час ставит `is_active=false`, Telegram менеджерам «Тур скрыт» (outbox) |
| **Регулярный тур** (`is_regular`) | Горящий/скрытие по дате **не** применяются |

Скидка **не выдумывается**: поле `hot_discount_percent` в админке тура (0 по умолчанию = только тег без скидки). Владелец задаёт процент для каждого тура отдельно.

Логика: `domain.TourCatalogContext`, `Tour.IsBurningOn` (`BurningWindowDays=7`), `TourLifecycleService`, worker `cmd/worker`. Бронь считает сумму с учётом скидки в окне горящего тура.

---

## Этап 15 — навигация UX (кнопка «Назад», крошки)

После этапа 14. Только frontend, без новых маршрутов.

Сделано:

- **Smart back** на детальных страницах: `/news/[slug]`, `/tours/[id]`, юридические документы — `router.back()` при in-app history, иначе fallback (`/news`, `/search`, `/legal`).
- `NavigationTracker` в layout — sessionStorage для SPA-переходов без referrer.
- **Крошки** на списках (новости, отзывы, поддержка, legal) и в личном кабинете.
- **Активный пункт** в header и mobile nav (`isMainNavLinkActive`).
- **Return to list** внизу длинных карточек (новость, тур).
- `PageIntro` и auth-страницы переведены на `SmartBackButton`.
- Форма заявки на `/tours/[id]`: sticky **сверху**, обе колонки скроллятся вместе.

---

## Этап 16 — цена на витрине и статус оплаты

Сделано (goose **00029**, [DECISIONS.md](DECISIONS.md) §18): цена видна только если > 0; иначе блок цены скрыт (и у регулярного, и у датированного). У заявки поле `payment_status`: `UNPAID` / `AWAITING_PAYMENT` / `PAID` / `NOT_REQUIRED`. Ручное редактирование в админке и показ в кабинете. **`PAYMENT_ADAPTER=noop`**.

---

## Этап 17 — тур «Тихвинский путь» и CTA каталога

Сделано (goose **00031**, PR #42 / #45): карточка `tikhvin-path` (регулярный + популярный, цена 0). Кнопка «Записаться на маршрут» → `/search?route=tikhvin-path`: сначала туры с этим именем/slug, ниже заголовок «Вас также могут заинтересовать другие туры». «Другие туры» → обычный `/search`.

---

## Этап 18 — места регулярного тура

Сделано (goose **00032**, PR #44): если в админке заполнили «Всего мест», а «Свободно» оставили 0, витрина **не** пишет «Мест нет» — берёт размер группы. Закрыть набор: снять **Активный**, не leftover=0.

---

## Этап 19 — мобильный UX (поиск, меню, превью новостей)

Сделано (PR #46):

- Главная: нативные `input type=date` не вылезают из белой карточки поиска.
- Бургер: пункт «Поддержка»; отдельного «Чат поддержки» нет (чат на `/support` и в кабинете).
- Лента `/news`: в карточке **одно** фото (первое); остальные — на странице статьи.

---

## Этап 2 — долг v2/v3 (не кабинет владельца)

Перенесено из STATUS / ROADMAP / V3_PLAN «не входит» / owner-setup. Код можно готовить, значения и live — нет.

| Остаток | Где | Что делать |
|---------|-----|------------|
| Листовки епархии | goose 22, потом админка | На проде **опубликованы как регулярные** без цены (Присвирье, Под покровом). Не назначать выдуманную цену. Датированный выезд — только если владелец даст дату и цену. |
| Регулярный тур: заявка с `total_price=0` | domain, этапы 13/16 | На витрине без цены. Внутреннюю цену не назначать, пока владелец не скажет. |
| Метрика | env `NEXT_PUBLIC_YM_ID` | На проде 111985266. Второй источник в админке **не** заводим. |
| Адаптеры paste-key | compose | Код есть; на проде `MESSENGER` / `PUBLISHER` / `AI` / `PAYMENT` = `noop`. |
| Max в уведомлениях | `adapters/notification/max` | Stub: даже с токеном send no-op. MessengerPort Max — отдельный порт. Не смешивать без просьбы. |
| Bitrix24 / 1С | `CRM_ADAPTER` / `ACCOUNTING_ADAPTER` | Live нет. |
| `DATA_MODEL` | docs | Поля v4 (`payment_status`, `hot_discount_percent`, likes/comments, `tour_views`) — в DATA_MODEL на 2026-08-27. |
| Инфра 4 vCPU / 8 ГБ | ВМ | Перед живым ботом / GPT / SMM в эфир, если ещё не сделано. Диск ≥80 ГБ. |

---

## Этап 3 — только владелец (не экраны-заглушки)

Клики и кабинеты, которые код не заменит. Список: [V4_OWNER_SETUP.md](V4_OWNER_SETUP.md).

- Нажать «Проверить» в Яндекс.Вебмастере и Google Search Console; отправить sitemap.
- Карточка в Яндекс Бизнесе / 2ГИС (те же имя, телефон, сайт).
- Ссылка с [tikhvin-eparhia.ru](https://www.tikhvin-eparhia.ru/).
- OAuth / SMTP / sms.ru callcheck — если env ещё пустые.
- MX / SPF / DKIM для `info@`.
- Telegram: получатели написали боту `/start`.
- Уникальные описания туров и реальные фото — в админке, тексты только ваши.
- Реквизиты оператора — вписать в `OPERATOR_*`, не выдумывать ИНН/ОГРН.

Не делаем фейковые админ-экраны «проверить Вебмастер».

---

## Этап 4 — live-адаптеры по просьбе

Код адаптеров уже в v3. Включение = `.env.production` + `make deploy`. Перед живым ботом/GPT — мощность ВМ (см. этап 2).

WhatsApp — только Cloud API. Неофициальные клиенты не подключаем.

---

## Этап 5 — оплата

`PaymentPort` (`sber` / `yookassa`) в коде, **`PAYMENT_ADAPTER=noop`**. Сумма = `booking.TotalPrice`. Возвратов нет.

В domain **нет** `AWAITING_PAYMENT` / `PAID`. Живую машину статусов не писать, пока владелец не подтвердит список. Кнопки Pay в кабинете нет.

---

## Этап 6 — юридичка

Код согласий (#22, goose 18–20) на проде. Тексты **не сертифицированы юристом**. `OPERATOR_*` после этапа 1 прокидываются compose; значения — владелец.

---

## Этап 7 — ИИ-звонки

Не этап 1. Нужны: телефония, промпты и правила владельца, живой `AI_ADAPTER` по просьбе. Не выдумывать цены и богословие. Эскалация человеку.

---

## Этап 8 — ИИ-продавец

Не этап 1. Автономный менеджер продаж — отдельное решение владельца после звонков и после живого GPT. Каталог только из опубликованных туров.

---

## Не входит в v4, пока владелец не попросит

- Live Bitrix24 / 1С
- Живой эквайринг без решения по статусам
- Возвраты, рассрочка
- Неофициальный WhatsApp
- Посты «из нейросети», фейковые визиты и отзывы, биржи ссылок
- СНИЛС
- Watchdog, который сам меняет прод
- Вторая ВМ, второй белый IP, новые порты security group
- Перенос `AWAITING_PAYMENT` / `PAID` в domain без текста владельца
- Репост в ленте новостей (этот срез: только лайки и комментарии)
- Вторая система логина «для ленты»
- Выдуманные title/description для SEO и выдуманные частоты Wordstat
- Дорвеи и посадочные под запрос без реального тура/новости
- Автодеплой feature-веток и PR на прод (только `main` после CI)
- `goose down` / `compose down -v` на проде

---

## Инфра (наследует v3)

Одна ВМ, снаружи **22 / 80 / 443**. Не `compose down -v`. После деплоя можно `docker builder prune`. Автодеплой с `main` — этап 12 (workflow есть, ключ SSH на 2026-08-27 не подходит). Пока ключ не исправлен — `make deploy`.

Перед живым ботом / GPT / SMM в эфир: **4 vCPU / 8 ГБ**, диск ≥80 ГБ.

---

## Definition of Done

- Решение владельца, не догадка
- Логика в domain/application, канал — адаптер
- Секреты в env, несекретное — «Настройки», если ключ уже в модели
- Тесты `go test` / `go vet` / `npm run lint` / `npm run build`
- Прод: мерж в `main` *должен* деплоить (этап 12); пока GHA SSH красный — `make deploy`. Postgres не сбрасывать
- Этап 1 не подменяет этапы 7–8
- Этап 12 не включает live Bitrix/оплату
- Этап 13/16: не выдумывать цену регулярного тура
- Не мержить устаревшие PR #25 и #27
