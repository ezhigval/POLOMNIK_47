# Roadmap

Канон: [AGENTS.md](../AGENTS.md). Решения: [DECISIONS.md](DECISIONS.md). Статус: [STATUS.md](STATUS.md).  
Владельцу: [V4_OWNER_SETUP.md](V4_OWNER_SETUP.md) · ранее [V2_OWNER_SETUP.md](V2_OWNER_SETUP.md).

Прод: https://tikhvin-palomnik.ru · freeze **`v3.0.0`** (2026-08-26, v3 code-complete + контент). Предыдущий: **`v2.1.0`**.  
v3 заморожен. Актуальный план: **[V4_PLAN.md](V4_PLAN.md)**. Этап 1 v4 = баги / следы ИИ / оптимизация. После него: автодеплой с `main` (этап 12), флаги туров (этап 13) и продукт сессия → компакт главной + SEO → лента `/news`. ИИ-звонки — не этап 1.

Правило: не выдумывать тексты, цены, статусы и юридичку. Bitrix24 / 1С live — только по просьбе владельца.

---

## Зафиксировано: v2.1.0 (2026-08-23)

Код на GitHub (`main`, тег `v2.1.0`) и на проде. Дальше — v3 (закрыт) и v4. Секреты и кабинеты — у владельца по чеклисту.

Сделано в коде:

- Настройки: имя/телефон/почта сайта; получатели уведомлений как **канал + адрес** (telegram / max / …)
- RBAC: полный админ = `ADMIN_TOKEN`; роли с хешем пароля, правами и привязкой по UUID
- Callcheck (sms.ru), Mailer, OAuth-кнопки с graceful disable
- SEO/analytics hooks, синяя палитра, CMS главной без фабрики страниц в UI
- Telegram live (нужен `/start` у получателей)
- Экран поддержки в админке + ответ менеджера; deep link в Telegram
- Восстановление пароля через Mailer (без SMTP — «пока недоступно»)

---

## Зафиксировано: v3.0.0 (2026-08-26)

Код на GitHub (`main`, тег `v3.0.0`) и на проде. Этапы v3 **0–10** закрыты кодом. Контентный срез: новости (святыни, икона в Москву), закрепление 1+2, флаг регулярного тура, Метрика **111985266**.

История этапов: [V3_PLAN.md](V3_PLAN.md) (закрыт, не удалять).

---

## v4 — текущая линейка

Полностью: **[V4_PLAN.md](V4_PLAN.md)**.

| Этап | Смысл | Статус |
|------|--------|--------|
| 0 | Freeze v3 как база | готово (`v3.0.0`) |
| 1 | Баги, следы ИИ, оптимизация | код этапа 1 (в `main`) |
| 9 | Сессия и cookies (вход + просмотренные туры, тот же кабинет) | не начат |
| 10 | Компактные новости на главной + SEO pass + Wordstat (запросы на живые страницы) | не начат |
| 11 | Лента `/news` ≈ VK: лайки + комментарии, без репоста; URL кликабельны; фото на весь экран | не начат (код не писать) |
| 12 | Автодеплой с `main` + goose только `up`, без потери данных | не начат |
| 13 | Флаги туров: активный / регулярный (без цены и даты) / популярный / овербукинг | **готово** |
| 2 | Долг v2/v3 без выдуманных правил | не начат |
| 3 | Кабинеты владельца (Вебмастер, OAuth, SMTP, карта, епархия) | не код |
| 4–6 | Live-адаптеры / оплата / юридичка | ждать владельца |
| 7–8 | ИИ-звонки, ИИ-продавец | **не этап 1** |

---

## После v3 — ждёт владельца (не разработка)

- [x] Метрика: `NEXT_PUBLIC_YM_ID=111985266`, вебвизор включён (код + prod env)
- [ ] Нажать «Проверить» в Яндекс.Вебмастере и Google Search Console; отправить sitemap
- [ ] Env: OAuth, `PHONE_ADAPTER`+`SMSRU_API_ID`, SMTP — если ещё пусто; GA по желанию — см. V4_OWNER_SETUP
- [ ] Telegram: получатели написали боту `/start`; smoke заявка / поддержка
- [ ] Почта: MX/SPF/DKIM под выбранный провайдер (`info@`)
- [ ] Цены и даты туров из листовок (пока скрыты); сумма заявки на регулярный тур = 0 — вопрос владельцу
- [ ] Одобренные отзывы, карта организации, ссылка с епархии — по SEO_ADS
- [ ] Live-адаптеры / эквайринг — только по явной просьбе; ЮKassa/Сбер **не** включены
- [ ] ВМ 4 vCPU / 8 ГБ перед живым ботом/GPT, если ещё не сделано

Не в v4, пока владелец не попросит: live Bitrix/1С, живой эквайринг без `AWAITING_PAYMENT` / `PAID`, линкфармы, фейковые отзывы, вторая ВМ.

---

## Definition of Done

- Решение владельца, не догадка
- Логика в domain/application, канал — адаптер
- Тесты, доки, тег при релизе
- Прод — только по просьбе; Postgres не сбрасывать
