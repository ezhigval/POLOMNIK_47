package telegram

// Message formatting and NotificationPort implementation live in
// application.NotificationDispatcher. This package only speaks Telegram HTTP
// (Client) and keeps legacy format helpers for unit tests of HTML payloads.
